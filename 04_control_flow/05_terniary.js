// Terniary Operator

// condition ? expressionIfTrue : expressionIfFalse;

let age = 20;
let allowed = age >= 18 ? 'Yes' : 'No';

console.log(allowed); // 'Yes'


let speed = 70;
let message = speed > 60 ? 'Too Fast' : (speed < 30 ? 'Too Slow' : 'Optimal');

console.log(message); // 'Too Fast'
